{
  "name": "node",
  "version": "1.0.0",
  "description": "const { error } = require(\"node:console\");\r const { create } = require(\"node:domain\");\r const fs = require(\"node:fs\");",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "chalk": "^5.6.2"
  },
  "devDependencies": {
    "axios": "^1.14.0"
  }
}
